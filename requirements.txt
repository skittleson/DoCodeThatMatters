bs4
requests
pybars3
rich
python-dotenv
# Kokoro-82M for blog post audio generation (voice: af_jessica, CUDA when available)
kokoro>=0.9.4
misaki[en]>=0.9.4
# misaki English G2P needs this spaCy model; ship it explicitly (uv venvs lack pip,
# so misaki's runtime auto-download via `python -m pip` would crash generation).
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
soundfile
# NOTE: ffmpeg must be installed on the system for WAV→MP3 conversion (apt install ffmpeg / brew install ffmpeg)