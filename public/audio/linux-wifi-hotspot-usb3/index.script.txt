Here is the spoken script:

Welcome back to our blog, where we're still working on a project to create a WiFi hotspot using PiHole and hostapd. I'm excited to share this setup with you, so be sure to come back for updates as we continue to work on it.

So, let's dive right in. In a nutshell, here's what you can expect: we'll use an Alfa AC1900 WiFi Adapter, which is connected via USB and comes equipped with four 5dBi external dual-band antennas. We'll also need to start by disabling power save mode and setting the transmission power for our adapter.

To do this, we have a starting script called `onstart.sh`. Let me walk you through what's inside it. First, we're turning off power save mode with a command like this: `/sbin/iw dev wlan0 set power_save off`. Then, we're setting the transmission power to 4500 milliwatts using another command: `/sbin/iw wlan0 set txpower fixed 4500`.

We also have some additional settings for our adapter. For example, we want to enable USB mode by writing a value of 1 to this file: `echo '1' > /sys/module/8814au/parameters/rtw_switch_usb_mode`. This will allow us to use the adapter in its highest performance mode.

The final part of our script sets up our WiFi hotspot using the create_ap project. We're creating a network with the name "TestNetwork" and setting the password to "testing123". You can find more information about this process on the blog post itself, where I'll have a link for you.

Now that we've set up our adapter and created our WiFi network, let's talk about disabling IPv6. This is an important step because it helps prevent any issues with your internet connection. To do this, simply open up the sysctl.conf file using `sudo nano /etc/sysctl.conf`. Inside, add these three lines of code to disable IPv6 for all interfaces:

`net.ipv6.conf.all.disable_ipv6=1`
`net.ipv6.conf.default.disable_ipv6=1`
`net.ipv6.conf.lo.disable_ipv6=1`

Finally, let's move on to setting up PiHole. This is a fantastic tool that helps you manage your DNS settings and block ads across all your devices. To get started with PiHole, simply run this command: `curl -sSL https://install.pi-hole.net | PIHOLE_SKIP_OS_CHECK=true sudo -E bash`.

Once the installation process is complete, be sure to set up your DHCP server to use the 192.168.12.1 range.

That's it for today! If you have any questions or need further clarification on these steps, don't hesitate to reach out in the comments below. I'll be back soon with another update on our project. Thanks for tuning in!
