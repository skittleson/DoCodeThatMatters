Here's the rewritten spoken script:

Let me show you how to build a Bluetooth proximity detector using a Raspberry Pi. This is going to be a simple guide, and it'll help us detect nearby Bluetooth devices without actually pairing them.

First, we need to make sure our USB Bluetooth adapter is working properly on the Raspberry Pi. We can do this by installing a package called `bluetooth` with the command `sudo apt-get install bluetooth`. Then, we check its status using `hcitool dev`. You should see something like "Devices: hci0 00:00:00:00:00:17", which is the device's Bluetooth address.

Next up, let's get the MAC address of a device. On an Android phone with version 6.0.1 or higher, you can find this in Settings > System > About device > Status > Bluetooth address.

Now that we have our devices' addresses, it's time to test the connection. We're going to use a command called `l2ping`, and we'll send two pings to the device with the MAC address `00:11:22:AA:BB:CC. The result should look something like this: "Ping: 00:11:22:AA:BB:CC from 00:00:00:00:00:17 (data size 44)...". You can find some additional commands and details in the full blog post, which is where I got most of these instructions.
