Here's the rewritten blog post in a spoken script format:

Hello there! I'm excited to share with you my journey of building a custom HTTP proxy in Rust for mixed-OS workflows. You can find the link on the blog post, but let me give you an overview.

I wanted to switch to a minimalist OS, but the catch was that my work didn't support Linux or a Mac at the time. I had to get creative. You see, I needed a way to run Linux full-time while still accessing my work's Windows environment seamlessly. It was a challenge, but I'm glad I took it on.

Now, let me tell you about the problem. I tried using Squid proxy first, but it worked mostly. But then I wrote a Python script to handle the proxying, which was better but felt hacky. That's when I decided to learn Rust and build a custom HTTP(S) forward proxy from scratch.

This is where things get interesting. I built a transparent HTTP(S) forward proxy in Rust that solves my specific problem: it supports both HTTP and HTTPS, has SSL/TLS error detection with helpful diagnostics, integrates with Windows firewall, and is cross-platform compatible. I've used this repo if you want to check it out.

Why Rust? Well, the performance has been said to be solid, and the async architecture with Tokio handles plenty of concurrent connections without causing any problems.

Now, let's get into the quick start guide. If you want to run the proxy on Linux or Windows, you can download the latest release from the repo I mentioned earlier and follow these steps:

1. Download the binary using wget.
2. Run it with the following command: `./rust_proxy-linux-x64 --host 0.0.0.0 --port 3129`

That's it! Point your system or app proxy settings to that host and port, and you're good to go.

Now, let's dive into the PopOS setup. The proxy was the easy part; getting PopOS to play nice with my work setup? That took some doing. I'll walk you through the Docker install process step by step.

First, update your system packages: `sudo apt update && sudo apt upgrade -y`. Then, install prerequisites: `sudo apt install -y apt-transport-https ca-certificates curl gnupg lsb-release`.

Next, add Docker's official GPG key: `sudo mkdir -p /etc/apt/keyrings; curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg`. Add the Docker repository: `echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null`.

Update and install: `sudo apt update; sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin`. Start and enable Docker: `sudo systemctl start docker; sudo systemctl enable docker`.

Finally, add your user to the Docker group: `sudo usermod -aG docker $USER`. Verify that everything is working as expected: `docker --version` and `docker compose version`.

Now, let's talk about GitHub account switching. I have both a personal and work GitHub account, and here's how I switch between them:

For work: `gh auth switch --user work_user`

For personal: `gh auth switch --user personal_user`

I know it would be nice if there was an alias or something, but this works.

Now, let's talk about Flatpak x11 fallback. Some Flatpak apps don't work well with Wayland, so here's how to force x11 for VSCodium:

`flatpak override --user --nosocket=fallback-x11 --socket=x11 com.vscodium.codium`

And finally, let's talk about .NET linking. I keep a local .dotnet install but link it to the system path:

`sudo ln -sf /home/spencerkittleson/.dotnet/dotnet /usr/bin/dotnet`

As for NVM for Node, you can simply run: `curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash`.

Now, let's talk about the PopOS fun window issues. You see, PopOS isn't perfect, and here are some things I had to fix.

First, let's talk about Gnome Tweaks. Adjust services startup by running: `gnome-session-properties`. Change workspaces quickly with `super + ctrl + up/down`, and restart Gnome shell by pressing `alt + f2` and typing "r", then hitting enter.

Next, let's talk about Bluetooth. I ditched the internal adapter since it was old and caused issues with PopOS all the time. To disable it, run: `hciconfig hci0 down`.

Now, let's talk about audio power saving. This is a common fix for audio issues:

`echo "0" | sudo tee /sys/module/snd_hda_intel/parameters/power_save`

Make it permanent by adding the following line to `/etc/modprobe.d/audio_disable_powersave.conf`: `options snd_hda_intel power_save=0`.

Next, let's talk about Wireplumber suspend. To prevent unwanted suspend, run: `sudo sed -i 's/--\["session.suspend-time-seconds\] = 5/\["session.suspend-time-seconds\] = 0' /usr/share/wireplumber/main.lua.d/50-alsa-config.lua` and then restart the pipewire service with `systemctl restart --user pipewire.service`.

Finally, let's talk about VSCodium GPU issues. If it's crashing, try running: `codium --disable-gpu`.

Now, let's get to the Chromium proxy setup. I run different work apps through different proxies. Here's how I launch Chromium with specific proxy settings:

`flatpak run --command=/app/bin/chromium org.chromium.Chromium --profile-directory=Default --proxy-server="http://192.168.8.204:3129;https://192.168.8.204:3129" & disown`

Or, if you want to ignore certificate errors:

`flatpak run --command=/app/bin/chromium org.chromium.Chromium --profile-directory=Default --proxy-server="http://192.168.8.204:3129;https://192.168.8.204:3129" --ignore-certificate-errors & disown`

I have multiple of these for different work contexts.

Now, let's talk about the router setup. I use a travel router to connect my Linux machine to the work Windows machine via ethernet. It's basically a home lab but for work. The router runs Wi-Fi 7, which is nice since my work laptops are from 2019 and don't support it.

So, after 15 months of using this setup, here's where I'm at: running PopOS full-time on my personal machine, work machine is only PopOS Cosmic, the Rust proxy handles all my traffic routing to a Windows PC, and I use the proxy to split internet traffic - work stuff goes through corp VPN, everything else direct.

In conclusion, I'd say keep it simple. A single, lightweight proxy can solve a lot of headaches without the bloat of a full VPN client. Don't over-optimize; focus on the part that actually matters to you. Document early; the notes you keep for yourself become the next blog post. And finally, stay curious; the tech landscape changes fast.

That's it! If you're juggling a corporate VPN and a personal workflow, a transparent forward proxy is a surprisingly powerful tool. It keeps your traffic private, gives you remote access to home services, and lets you sidestep the slowdowns of a full-tunnel VPN. Give it a shot and see how it changes your day-to-day dev life!
