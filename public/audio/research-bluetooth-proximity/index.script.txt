You're interested in doing some research on Bluetooth proximity using open tools. There's a related blog post I'd like to mention first, but we'll be focusing on the tools and techniques outlined here.

Let me walk you through some of the commands you can use with these tools. First, make sure to replace any MAC addresses that are mentioned with your own device's address.

To scan for Bluetooth Low Energy devices within a certain range, you would use `hcitool lescan` - this might take around 30 seconds to complete. Keep in mind that the timeout is set to 30 seconds, but you can adjust it if needed.

If you want to see the RSSI values of nearby Bluetooth LE devices, you'd run `btmgmt find`. When you do this, you'll get a list of devices with their respective signal strengths - these are expressed in decibels. The higher the value, the stronger the signal is.

Another useful command is `l2ping`, which allows you to ping a Bluetooth-enabled device without actually pairing it first. You can use it like this: `l2ping -c 3 <MAC address>`. Keep in mind that this will send three pings and then stop.

If you're interested in looking at the data being exchanged between two devices, you might find `hcidump` useful. It allows you to dump all of the data for a paired device. To use it, simply run `hcidump -a`.

Now, let's look at some more advanced techniques. If you want to request authorization from a device, you can use `hcitool cc <MAC>`, followed by `hcitool auth <MAC>`.

One interesting thing is that if you try to connect to a device and then immediately ask for its RSSI value, the connection won't be successful - but the RSSI will still be available. You can do this with the command `hcitool cc <MAC> && hcitool con && hcitool rssi <MAC>`.

To request just the RSSI value without attempting a connection, you would use `hcitool cmd 0x05 0x0005 0x00 0x00 <MAC>`.

If you want to learn more about using these tools or need help with anything else related to Bluetooth proximity research, I'd recommend checking out some of the resources listed here. Keep in mind that there are many more articles and tutorials available online - a quick search on your favorite search engine should yield plenty of results.
